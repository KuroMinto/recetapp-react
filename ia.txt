[Parte B]
[Primera etapa]

Prompt:
Importa los datos de src/data/recetas hacia app.tsx y muestralos mediante la consola.
Respuesta: Importé los datos en `App.tsx` desde `src/data/recetas.ts` y los muestro con `console.log`.
Utilidad:
Modificaciones del usuario:

Prompt:
crea un archivo llamado "ia.txt" que contenga cada prompt que haya dado y que te de desde ahora en adelante, con el siguiente formato:

    Prompt:
    Respuesta: (Esta debe ser resumida)
    Utilidad: (deja esto vacío, lo rellenaré yo)
    Modificaciones del usuario: (Deja esto vacío)

Respuesta: Creé el archivo `ia.txt` con el formato solicitado y comienzo el registro de prompts.
Utilidad:
Modificaciones del usuario:

[Segunda etapa]

Prompt:
Lo siguiente corresponde a la segunda etapa del proyecto:

1. Crea src/components/RecetaCard.jsx. 
2. El componente recibe por props: nombre, origen, porciones, categoria, descripcion, ingredientes. 
3. Muestra la información de forma visual y ordenada. 
4. Aplica un estilo diferenciado según la categoría (un color/etiqueta distinto para Entrada, Fondo, Postre).

Recuerda que debes actualizar ia.txt según el formato que te di con anterioridad.
Respuesta: Creé `src/components/RecetaCard.jsx`, actualicé `App.tsx` para renderizar tarjetas de recetas y agregué estilos diferenciados por categoría.
Utilidad:
Modificaciones del usuario:

Prompt:
recuerda quitar lo que venía por default en la app.tsx
Respuesta: Eliminé el contenido predeterminado de Vite en `App.tsx` y dejé solo la sección de recetas.
Utilidad:
Modificaciones del usuario:

[Tercera etapa]

Prompt:
Ahora pasamos a la tercera etapa:

1. Crea src/components/ListaRecetas.jsx que reciba el array de recetas por props. 
2. Recorre el array con .map() y renderiza un RecetaCard por cada receta. Programación Front End (TI3031) — Sumativa U3 · V4 Página 5 
3. Asigna correctamente la prop key usando el id de cada elemento (no uses el índice del array). 
4. En App.jsx, renderiza ListaRecetas pasándole los datos importados.
Respuesta: Creé `src/components/ListaRecetas.jsx`, actualicé `App.tsx` para renderizar `ListaRecetas` con `recetas` importadas y usé `id` como key.
Utilidad:
Modificaciones del usuario:

Prompt:
verifica errores
Respuesta: Revisé errores y corregí el TS config para permitir archivos `.jsx` importados desde TypeScript.
Utilidad:
Modificaciones del usuario:

[Cuarta etapa]

Prompt:
Ahora pasamos a la cuarta etapa:

1. Crea src/components/FiltroCategoria.jsx con botones o un selector para filtrar por la categoría: “Todas”, “Entrada”, “Fondo”, “Postre”. 
2. En App.jsx, usa useState para guardar el filtro seleccionado y un campo de búsqueda por nombre. 
3. Aplica ambos filtros sobre el array antes de pasarlo a ListaRecetas. 
4. Usa renderizado condicional para mostrar el mensaje “No hay recetas que coincidan” cuando el resultado quede vacío.

Modifica el archivo IA desde [Cuarta etapa]
Respuesta: Creé `src/components/FiltroCategoria.jsx`, agregué filtros de categoría y búsqueda en `App.tsx`, apliqué ambos filtros antes de renderizar `ListaRecetas` y mostré el mensaje de no resultados.
Utilidad:
Modificaciones del usuario:

Prompt:
Implementa estas buenas prácticas para esta etapa:

• El campo de búsqueda debe ser un componente controlado (su valor vive en el estado de React). 
• Valida y normaliza el texto de búsqueda (recorta espacios y limita el largo) antes de usarlo, para no confiar ciegamente en la entrada del usuario.
Respuesta: Convertí el campo de búsqueda en un componente controlado y normalicé el texto con trim y slice antes de aplicar el filtro.